on:
  create:
  issues:
    types: [opened]
  push:
    branches:
      - master

jobs:
  foobar:
    name: Foobar
    runs-on: ubuntu-latest
    steps:
      - env:
          GITHUB_EVENT: ${{ toJSON(github.event) }}
        run: echo "$GITHUB_EVENT"
